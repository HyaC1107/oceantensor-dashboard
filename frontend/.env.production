# 프로덕션 빌드 전용 — `npm run build`(production 모드)가 .env(로컬 dev)를 덮어쓴다.
# 배포: oceantensor.ai.kr/app · API/WS는 nginx /hbapi/ 리버스프록시(같은 오리진 상대경로)
# 🚫 시크릿 금지(경로만). base(/app/)는 빌드 시 VITE_BASE=/app/ 로 전달.
VITE_API_BASE_URL=/hbapi
VITE_WS_URL=/hbapi/ws/sensor
